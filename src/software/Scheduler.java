package software;

public class Scheduler extends Thread{
    //fila de prontos (semáforo) 
    //fila de blocked (semáforos)
    //fila de pedidos console (semáforo)
    //gerente de processos (semáforo)x
}
